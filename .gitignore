# credentials — never commit
ftp.txt

# private family journal backup (tools/pull_journal.py) — the GitHub repo is public
journal-backup/

# local tooling / caches
.venv/
__pycache__/
*.pyc
.deploy-manifest.json
.claude/

# full-size photo originals kept by tools/shrink_photos.py
photos/_originals/

# OS noise
Thumbs.db
.DS_Store
